# gRPC Service

> **WIP**

The sb-stack gRPC service will provide:

- HTTP/2 transport integration
- Protocol Buffers serialization
- Streaming support (unary, server streaming, client streaming, bidirectional)
- Type-safe service definitions
- Middleware support
